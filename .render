services:
  - type: web
    name: youtube-stream-video1
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python stream.py
